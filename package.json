{
  "name": "fertility-app",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "web": "npm run dev --workspace=apps/web",
    "mobile": "npm run start --workspace=apps/mobile",
    "backend": "npm run start:dev --workspace=apps/backend",
    "build:web": "npm run build:shared && npm run build --workspace=apps/web",
    "build:backend": "npm run build:shared && npm run build --workspace=apps/backend",
    "build:shared": "npm run build --workspace=packages/shared",
    "build:all": "npm run build:shared && npm run build --workspace=apps/backend && npm run build --workspace=apps/web"
  },
  "devDependencies": {
    "typescript": "^6.0.0"
  }
}
